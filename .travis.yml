sudo:
    false

language: 
    node_js

node_js:
    - '13'

# dist: 
#     trusty
    
# service:
#     mongodb

# env:
#     - NODE_ENV=ci PORT=5000

cache:
    directories:
        - npm

# before_install:
#     - sudo apt-get install -y libxml2-dev
    
install:
    - cd ./app/server && npm ci

script:
    - npm run test-ci